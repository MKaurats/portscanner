import socket
from IPy import IP

ipaddress = input('Input IP address to preform scan: ')
port = 80

try:
	sock  = socket.socket()
	sock.connect((ipaddress, port))
	print('Port is Open')
except:
	print('Port is Closed')
